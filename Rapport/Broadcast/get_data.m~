load output.txt
thread_1=output;
%%
load braodcast.mat

close all
figure
hold on
stem(0:1:length(thread_1)-1,thread_1,'r')
plot(0:1:length(thread_10)-1,thread_10,'g')
stem(0:1:length(thread_20)-1,thread_20,'y')
plot(0:1:length(thread_40)-1,thread_40,'b')
xlabel('Thread ID');
ylabel('Time [us]');